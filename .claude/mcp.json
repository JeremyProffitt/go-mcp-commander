{
  "mcpServers": {
    "commander": {
      "command": "${workspaceFolder}/go-mcp-commander.exe",
      "args": [
        "-log-dir", "${workspaceFolder}/logs",
        "-log-level", "info"
      ]
    }
  }
}
